---
const { src, artist, work, source, genre } = Astro.props;
---

<div class=`filter ${genre} ${source}`>
    <a class="visit" href={`${src}`}
        >{artist}<br />
        <span class="secondary" style="font-style: italic;">&#10149; {work} </span></a
    > 
</div>
