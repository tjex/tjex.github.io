---
import * as colUtil from "../scripts/collectionUtil.ts";

const { collection } = Astro.props;

const entries = await colUtil.noDrafts(collection);
const tags = [...new Set(entries.map((entry: any) => entry.data.tags).flat())];
---

<p>
{
    tags.map((tag) => (
        <a class="tag" href={`/tags/${tag}`}>
            {tag}
        </a>
    ))
}
</p>
